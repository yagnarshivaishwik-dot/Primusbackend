<UserControl x:Class="PrimusKiosk.App.Views.ChatPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PrimusKiosk.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ChatViewModel}">
  <Border Background="{StaticResource SurfaceBrush}"
          BorderBrush="{StaticResource BorderSoftBrush}"
          BorderThickness="1"
          CornerRadius="14"
          Padding="12">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <TextBlock Text="Chat" Style="{StaticResource TitleText}" FontSize="16" Margin="0,0,0,8" />

      <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Messages}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Margin="0,4"
                      Padding="10,6"
                      CornerRadius="10"
                      Background="{StaticResource Slate800}">
                <StackPanel>
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Sender}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding TimestampUtc, StringFormat='  •  {0:t}'}"
                               Style="{StaticResource MutedText}" />
                  </StackPanel>
                  <TextBlock Text="{Binding Body}" TextWrapping="Wrap" Margin="0,2,0,0" />
                </StackPanel>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>

      <Grid Grid.Row="2" Margin="0,8,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0"
                 Text="{Binding Draft, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0" />
        <Button Grid.Column="1"
                Content="Send"
                Command="{Binding SendCommand}"
                Margin="8,0,0,0" />
      </Grid>
    </Grid>
  </Border>
</UserControl>
