<UserControl x:Class="PrimusKiosk.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PrimusKiosk.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:LoginViewModel}">
  <Grid>
    <Border Style="{StaticResource CardSurface}"
            MaxWidth="460"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
      <StackPanel>
        <!-- Gradient logo circle -->
        <Border Width="72" Height="72" CornerRadius="36" HorizontalAlignment="Center" Margin="0,0,0,16">
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
              <GradientStop Color="#6366F1" Offset="0" />
              <GradientStop Color="#4338CA" Offset="1" />
            </LinearGradientBrush>
          </Border.Background>
          <TextBlock Text="&#xE8AD;"
                     FontFamily="{StaticResource IconFontFamily}"
                     FontSize="32"
                     Foreground="White"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center" />
        </Border>

        <TextBlock Text="Primus Gaming Cafe"
                   FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" />
        <TextBlock Text="Welcome back! Please sign in to continue."
                   Style="{StaticResource MutedText}"
                   HorizontalAlignment="Center"
                   Margin="0,4,0,28" />

        <TextBlock Text="Email or username" Style="{StaticResource MutedText}" Margin="0,0,0,4" />
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Password" Style="{StaticResource MutedText}" Margin="0,12,0,4" />
        <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" />

        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource DangerBrush}"
                   FontSize="12"
                   Margin="0,12,0,0"
                   TextWrapping="Wrap" />

        <Button Content="{Binding LoginButtonText}"
                Command="{Binding LoginCommand}"
                Margin="0,20,0,0"
                Height="44"
                FontSize="15" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
