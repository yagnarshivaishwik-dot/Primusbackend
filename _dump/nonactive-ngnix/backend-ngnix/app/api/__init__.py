"""
API package for FastAPI endpoints.

This file exists primarily to give tools like mypy a stable package
hierarchy (`app.api.*`) and avoid treating `api` as a separate top-level
package when running type checks.
"""
